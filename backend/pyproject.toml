[project]
name = "knowledge-tree-backend"
version = "0.1.0"
description = "Knowledge Tree visualization backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
